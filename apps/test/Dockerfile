# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @test/test`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @test/test`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @test/test && \
          adduser --system -G @test/test @test/test

COPY apps/test/dist @test/test/
RUN chown -R @test/test:@test/test .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @test/test --omit=dev -f install

CMD [ "node", "@test/test" ]
